name: CI Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  # ────────────────────────────────────────────────
  # Phase 1 : Build de l'image Docker
  # ────────────────────────────────────────────────
  build:
    name: Build Docker Image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: false
          tags: wallet-pfa:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ────────────────────────────────────────────────
  # Phase 2 : SAST + scan de secrets  (à venir)
  # ────────────────────────────────────────────────
  # sast:
  #   name: SAST & Secret Scanning
  #   needs: build
  #   steps:
  #     - uses: gitleaks/gitleaks-action@v2       # détecte SECRET_KEY hardcodé
  #     - run: pip install bandit && bandit -r app  # analyse statique Python

  # ────────────────────────────────────────────────
  # Phase 3 : Scan CVE / SCA  (à venir)
  # ────────────────────────────────────────────────
  # sca:
  #   name: CVE & Dependency Scan
  #   needs: build
  #   steps:
  #     - uses: aquasecurity/trivy-action@master   # détecte requests==2.18.0
  #     - run: pip install pip-audit && pip-audit

  # ────────────────────────────────────────────────
  # Phase 4 : Déploiement Kubernetes  (à venir)
  # ────────────────────────────────────────────────
  # deploy:
  #   name: Deploy to K8s
  #   needs: [sast, sca]
  #   steps:
  #     - run: kubectl apply -f k8s/
